"""
文件说明：instruction.py - 任务指令配置模块

本文件主要功能：
1. 定义各任务的详细指令描述(TASK_TO_INSTRUCTION)
2. 定义各任务的最大生成token数配置(TASK_TO_MAX_NEW_TOKENS)
3. 定义各任务的简化任务类型(TASK_TO_TASK_TYPE)

支持的任务类型（共16种医疗NLP任务）：
1. 信息抽取类：CMeEE-V2(命名实体识别), CMeIE(关系抽取), IMCS-V2-NER, CHIP-CDEE
2. 文本分类类：CHIP-CTC, KUAKE-QIC, IMCS-V2-DAC
3. 文本匹配类：CHIP-STS, KUAKE-IR, KUAKE-QQR, KUAKE-QTR
4. 序列生成类：IMCS-V2-MRG, MedDG
5. 属性判断类：CHIP-MDCFNPC, IMCS-V2-SR
6. 实体标准化：CHIP-CDN

这些配置用于：
- 控制模型生成的长度
- 为模型提供任务理解的上下文
- 区分不同类型的NLP任务

作者：项目开发团队
"""

import pandas as pd
import numpy as np

from torch.utils.data import Dataset, DataLoader
from datasets import load_dataset
import datasets


# =========================================
# 各任务的详细指令描述
# 用于引导模型理解具体任务要求
# =========================================
TASK_TO_INSTRUCTION = {
    # CHIP系列任务
    "CHIP-CDEE" : "从下列输入中进行临床发现事件抽取任务。输出临床发现事件的主体词，以及发生状态，描述词和解剖部位这三种属性，其中描述词和解剖部位可能有多个值",
    "CHIP-CDN" : "诊断实体的语义标准化, 从给定的实体选项中选择与原诊断描述匹配的诊断标准词。从实体选项候选输出结果" ,
    "CHIP-CTC" : "根据输入的句子，确定该句子描述的临床试验筛选标准所属的类型。从类型选项候选输出结果",
    "CHIP-MDCFNPC" : "阴阳性判断的任务，在对话中，给出了一系列临床发现实体，然后根据每个实体判断其阴性或阳性。实体包括症状、疾病或假设可能发生的疾病，以及其他医学检查结果。根据对话内容，需要判断每个实体是已有症状疾病、未患有症状疾病，或者回答不明确或无实际意义。",
    
    "CHIP-STS": "判断输入中的两句话的意思是否相同。如果两句话意思相同输出\"是的\",意思不相同输出\"不是\"",
    
    # CMeEE和CMeIE任务
    "CMeEE-V2" : "抽取出输入中的医学相关命令实体，并根据提供的选项选择特定类型的实体列表。",
    "CMeIE" :  "从给定的文本中找出特定类型的关系，并找出关系的头实体和尾实体。对每个特定关系三元组输出格式，具有**关系的头尾实体对如下：头实体为**，尾实体为**。如果没有找到实体对。输出\"没有指定类型的三元组\". " ,
    
    # IMCS系列任务（智能医疗对话系统）
    "IMCS-V2-DAC" : "判断输入中给定的问诊句子或陈述句的意图类型。根据所提供的选项，选择输出与句子意图相匹配的答案。", 
    "IMCS-V2-MRG" : "根据下输入中给定的问诊对话生成诊疗报告。输出报告需要包括主诉，现病史，辅助检查，既往史，诊断，建议的内容",
    "IMCS-V2-NER" : "根据给定的输入文本，输出对应的实体类型和实体名称。如果没有找到实体对。输出\"上述句子没有指定类型实体\"",
    "IMCS-V2-SR" : "根据给定的对话历史和当前对话，输出每个对话中涉及的症状以及这些症状的阴阳性判断。如果患有该症状输出\"阳性\",没有患有该症状输出\"阴性\",无法根据上下文确定病人是否患有该症状输出\"无法确定\"",
    
    # KUAKE系列任务（医疗搜索相关）
    "KUAKE-IR" : "判断输入中的医疗搜索和回答内容是否相关。如果内容相关输出\"相关\",内容不相关输出\"不相关\"",
    "KUAKE-QIC" : "根据输入中的搜索内容句子，判断搜索的意图类型, 从类型选项候选输出结果",
    "KUAKE-QQR" : "判断输入两个句子之间的语义包含关系。是\"完全一致\"，\"后者是前者的语义子集\"，\"后者是前者的语义父集\"，\"语义无直接关联\"的哪一种",
    "KUAKE-QTR" : "判断输入两个句子之间的语义相似程度。是\"完全不匹配或者没有参考价值\"，\"很少匹配有一些参考价值\"，\"部分匹配\"，\"完全匹配\"中的哪一种" ,
    
    # MedDG任务（医学对话生成）
    "MedDG" : "根据输入中给定的问诊对话历史生成医生的下一句回复"
}

# =========================================
# 各任务的最大生成token数配置
# 根据不同任务的输出复杂度设置合适的长度
# =========================================
TASK_TO_MAX_NEW_TOKENS = {
    "CHIP-CDEE" : 256,      # 临床发现事件抽取：输出多个事件及其属性
    "CHIP-CDN" :  128,      # 诊断标准化：输出匹配的标准诊断词
    "CHIP-CTC" : 10,        # 临床试验分类：输出分类标签
    "CHIP-MDCFNPC" : 512,   # 阴阳性判断：输出多个实体的判断结果
    "CHIP-STS": 4,          # 语义相似度：输出是/否判断
    "CMeEE-V2" : 150,       # 医学实体识别：输出多个实体及类型
    "CMeIE" :  256,         # 医学关系抽取：输出关系三元组
    "IMCS-V2-DAC" : 32,     # 意图识别：输出意图类别
    "IMCS-V2-MRG" : 150,    # 诊疗报告生成：输出完整报告
    "IMCS-V2-NER" : 64,     # 对话实体识别：输出实体列表
    "IMCS-V2-SR" : 64,      # 症状识别：输出症状及阴阳性
    "KUAKE-IR" : 7,         # 信息检索相关性：输出相关/不相关
    "KUAKE-QIC" : 10,       # 查询意图分类：输出意图类别
    "KUAKE-QQR" : 16,       # 查询相关性：输出语义关系
    "KUAKE-QTR" : 16,       # 查询匹配度：输出匹配程度
    "MedDG" : 100,          # 医学对话生成：生成医生回复
}

# =========================================
# 各任务的简化任务类型
# 用于在输入前添加简短的任务标识，帮助模型快速识别任务类型
# =========================================
TASK_TO_TASK_TYPE = {
    "CHIP-CDEE" : "事件抽取",            # 从临床文本中抽取事件及其属性
    "CHIP-CDN" :  "多项选择",            # 从候选项中选择正确的标准化诊断
    "CHIP-CTC" : "文本分类",             # 对临床试验筛选标准进行分类
    "CHIP-MDCFNPC" : "实体阴阳性分析",   # 判断症状/疾病的阴阳性
    "CHIP-STS" : "语义匹配",             # 判断两句话语义是否相同
    "CMeEE-V2" : "实体识别",             # 识别医学文本中的命名实体
    "CMeIE" :  "实体三元组抽取",         # 抽取实体间的医学关系
    "IMCS-V2-DAC" : "意图识别",          # 识别问诊对话的意图
    "IMCS-V2-MRG" : "生成式摘要",        # 根据对话生成诊疗报告
    "IMCS-V2-NER" : "实体识别",          # 从对话中识别实体
    "IMCS-V2-SR" : "实体识别并文本分类", # 识别对话中的症状及状态
    "KUAKE-IR" : "相关性分类",           # 判断搜索与回答的相关性
    "KUAKE-QIC" : "意图分类",            # 对搜索查询进行意图分类
    "KUAKE-QQR" : "自然语言推理",        # 判断两个查询的语义关系
    "KUAKE-QTR" : "语义匹配",            # 判断查询与结果的匹配程度
    "MedDG" : "生成回复",                # 生成医学对话回复
}